# Domain Documentation

This repository uses a single-context layout.

- Read `CONTEXT.md` at the repository root for domain terminology,
  boundaries, invariants, and workflows.
- Read `docs/adr/` for architectural decisions.
- Add new architectural decisions under `docs/adr/`.
- Update `CONTEXT.md` when the domain model or shared vocabulary changes.
