# Issue Tracker

Issues are tracked in GitHub Issues for the repository configured as the
`origin` remote.

Use the GitHub CLI (`gh`) to read, create, update, label, and close issues.
Determine the repository from `git remote get-url origin`.

Pull requests are not treated as incoming triage requests by default.
